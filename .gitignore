node_modules
dist
.DS_Store
*.log
.env
.fly

# Runtime state: venues.json (falls back to the single demo venue) and per-venue
# floor plans (fall back to defaultPlan()).
data/
.playwright-mcp

# Playwright output
test-results/
playwright-report/
